class Example{
    init(){ this.draw(); }
    draw(){}

    keydown(event){}
    keyup(event){};
    mousedown(event){}
    mouseup(event){}
    mousemove(event){}

    constructor(name = 'example'){
        this.name = name;
    }
}
